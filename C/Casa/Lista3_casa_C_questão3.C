#include <stdio.h>

int main() {
    int(cont);
    cont=1;
    while(cont<=20){
        if(cont%2==0){printf("%i: par;\n", cont);}
        else{printf("%i: ímpar;\n", cont);}
        cont++;
    }
}